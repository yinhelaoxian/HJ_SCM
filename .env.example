# HJ_SCM 环境配置模板
# ========================
# 1. 复制此文件为 .env
# 2. 填写真实配置值
# 3. 不要将 .env 提交到版本控制！
# ========================

# ---------------- 数据库配置 ----------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres        # 生产环境使用强密码
POSTGRES_DB=hjscm

DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres             # 生产环境使用强密码

# ---------------- Redis 配置 ----------------
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=                  # 如需认证，取消注释并设置密码

# ---------------- 应用配置 ----------------
SPRING_PROFILES_ACTIVE=dev      # dev | test | prod

# ---------------- JWT 安全配置 ----------------
# ⚠️ 生产环境必须设置复杂的随机字符串！
# 生成方式：openssl rand -base64 64
JWT_SECRET=                     # 设置为复杂的随机字符串（至少 256 位）
JWT_EXPIRATION=28800000         # Access Token 过期时间（毫秒），默认 8 小时

# ---------------- 其他配置 ----------------
# JAVA_OPTS=-Xmx512m           # JVM 内存配置
