# 解决方案建议书 v1.0

**版本**: v1.0  
**日期**: 2026-02-15  
**来源**: 系统评估报告  
**目标**: 从"演示原型"升级为"可运营系统"

---

## 一、评估结论回顾

### 1.1 综合评分

| 评估维度 | 满分 | 得分 | 等级 |
|----------|------|------|------|
| 菜单架构覆盖度 | 100 | 72 | ✅ 合理 |
| 功能内容实现深度 | 100 | 31 | ❌ 严重不足 |
| 数据联通与流程闭环 | 100 | 22 | ❌ 基本断路 |
| 企业业务场景适配度 | 100 | 45 | ⚠️ 一般 |
| **综合加权** | **100** | **39** | **❌ 不达标** |

### 1.2 五大致命缺陷

| # | 缺陷 | 影响 | 优先级 |
|---|------|------|--------|
| 1 | 主数据管理（MDM）完全缺失 | BOM、物料、供应商无管理 | 🚨 P0 |
| 2 | 质量管理（QM）完全空白 | IQC/OQC/NCR/CAPA 全缺失 | 🚨 P0 |
| 3 | WMS 仓库作业完全缺失 | 收货/上架/拣货/盘点无实现 | 🚨 P0 |
| 4 | 端到端数据流断裂 | 全链路依赖 mock | 🔴 P1 |
| 5 | ERP 集成零实现 | 无法与外部系统对接 | 🔴 P1 |

---

## 二、解决方案总览

### 2.1 三阶段整改路线图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Phase 1: 基础补全（1个月）                      │
│  目标：建立系统运行的基础能力，消除上线阻碍                            │
│  - 主数据管理（MDM）                                                  │
│  - 质量管理（QM）                                                     │
│  - WMS 基础作业                                                      │
├─────────────────────────────────────────────────────────────────────┤
│                        Phase 2: 核心打通（2-3个月）                   │
│  目标：打通端到端数据流，实现业务闭环                                 │
│  - 生产执行完整化                                                    │
│  - ERP 系统集成                                                      │
│  - MRP 引擎完善                                                      │
├─────────────────────────────────────────────────────────────────────┤
│                        Phase 3: 高级能力（3-6个月）                   │
│  目标：差异化竞争力，支撑豪江战略                                     │
│  - 多工厂协同                                                        │
│  - 逆向物流                                                         │
│  - 合规管理                                                         │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 投资回报分析

| 阶段 | 投入 | 预期产出 | ROI |
|------|------|----------|-----|
| Phase 1 | 3人 × 1月 | 可运营基础 | - |
| Phase 2 | 5人 × 2月 | 业务闭环 | 3个月见效 |
| Phase 3 | 4人 × 3月 | 竞争优势 | 6个月见效 |

---

## 三、Phase 1: 基础补全方案

### 3.1 主数据管理（MDM）

#### 3.1.1 问题诊断

| 当前状态 | 问题 | 影响 |
|----------|------|------|
| 无 BOM 管理 | 3-5 层 BOM 无法展开 | MRP 运算无效 |
| 无物料主数据 | 物料编码/参数缺失 | MRP/生产无依据 |
| 供应商档案不完整 | 仅 3 家 mock 数据 | 采购/招标无支撑 |
| 工艺路线缺失 | 5 道工序无法追踪 | 生产排程不准 |

#### 3.1.2 解决方案

**建立 MDM 核心模块：**

```typescript
// 物料主数据模块
interface MaterialMaster {
  materialId: string;          // 物料编码
  materialName: string;       // 物料名称
  materialGroup: string;      // 物料组
  unit: string;               // 计量单位
  // 采购参数
  supplier: string;           // 默认供应商
  moq: number;                // 最小订单量
  lt: number;                 // 提前期(天)
  priceRange: [number, number]; // 价格区间
  // 生产参数
  routingId: string;          // 工艺路线
  batchSize: number;          // 批量大小
  scrapRate: number;           // 损耗率
  // 库存参数
  storageLocation: string;     // 默认库位
  abcClass: 'A' | 'B' | 'C'; // ABC分类
}

// BOM 多层级结构
interface BOM {
  bomId: string;
  parentMaterial: string;      // 父级物料
  components: BOMComponent[];  // 子件列表
  bomLevel: number;           // BOM层级
  effectiveDate: Date;        // 生效日期
  alternativeBoms: string[];  // 替代BOM
}

interface BOMComponent {
  componentMaterial: string;   // 子件物料
  quantity: number;           // 用量
  operationId: string;       // 工序号
  ScrapAddRate: number;      // 损耗率
}
```

#### 3.1.3 实施计划

| 周次 | 任务 | 交付物 |
|------|------|--------|
| Week 1 | 物料主数据建模 | 数据模型文档 |
| Week 2 | BOM 管理界面 | BOM 维护页面 |
| Week 3 | 供应商档案管理 | 供应商管理页面 |
| Week 4 | 工艺路线配置 | 工艺路线页面 |

---

### 3.2 质量管理（QM）

#### 3.2.1 问题诊断

| 缺失功能 | 业务影响 | 豪江场景 |
|----------|----------|----------|
| IQC 来料检验 | Bühler 电机质量问题无法拦截 | 唯一供应商，质量风险高 |
| OQC 出货检验 | 瑞哈医疗产品无质量记录 | 医疗器械合规要求 |
| NCR/CAPA | 质量问题无处理闭环 | 持续改进无法落地 |

#### 3.2.2 解决方案

**建立质量管理模块：**

```typescript
// IQC 来料检验
interface IQCInspection {
  inspectionId: string;
  purchaseOrder: string;      // 采购订单
  material: string;          // 物料
  supplier: string;          // 供应商
  batchNo: string;           // 批次号
  quantity: number;          // 到货数量
  inspectionDate: Date;
  inspector: string;
  items: InspectionItem[];   // 检验项目
  result: 'PASS' | 'FAIL' | 'HOLD';
  ncrId?: string;           // 不合格品处理单
}

// OQC 出货检验
interface OQCInspection {
  inspectionId: string;
  salesOrder: string;        // 销售订单
  material: string;          // 物料
  customer: string;          // 客户
  inspectionDate: Date;
  items: InspectionItem[];
  result: 'PASS' | 'FAIL';
  certificate?: string;      // 质量证书
}

// 不合格品处理
interface NCR {
  ncrId: string;
  source: 'IQC' | 'OQC' | 'IPQC';
  material: string;
  quantity: number;
  issue: string;             // 问题描述
  disposition: 'RETURN' | 'REWORK' | 'SCRAP' | 'ACCEPT';
  rootCause: string;         // 根本原因
  correctiveAction: string;  // 纠正措施
  CAPAId?: string;          // CAPA 关联
}
```

#### 3.2.3 实施计划

| 周次 | 任务 | 交付物 |
|------|------|--------|
| Week 1 | IQC 来料检验流程 | IQC 检验页面 |
| Week 2 | OQC 出货检验流程 | OQC 检验页面 |
| Week 3 | NCR 不合格品处理 | NCR 处理页面 |
| Week 4 | CAPA 闭环管理 | CAPA 管理页面 |

---

### 3.3 WMS 仓库作业

#### 3.3.1 问题诊断

| 缺失功能 | 业务影响 | 豪江场景 |
|----------|----------|----------|
| 收货作业（GR） | PO 无法入库 | 采购订单无法闭环 |
| 库位管理 | 库存精度低 | 三厂仓库无法区分 |
| 拣货发货 | 发货效率低 | 订单无法履约 |
| 盘点管理 | 账实差异大 | 库存数据不可信 |

#### 3.3.2 解决方案

**建立 WMS 核心模块：**

```typescript
// 收货作业
interface GoodsReceipt {
  grId: string;
  poNumber: string;
  supplier: string;
  items: GRItem[];
  status: 'RECEIVING' | 'CHECKED' | 'PUTAWAY' | 'COMPLETED';
  receivedDate: Date;
}

interface GRItem {
  material: string;
  expectedQty: number;
  actualQty: number;
  batchNo: string;
  inspectionStatus: 'PENDING' | 'PASSED' | 'FAILED';
}

// 上架管理
interface PutAway {
  putawayId: string;
  grId: string;
  items: PutAwayItem[];
  storageBin: string;        // 目标库位
  status: 'PENDING' | 'IN_PROGRESS' | 'COMPLETED';
}

// 库存管理
interface Inventory {
  material: string;
  storageLocation: string;
  storageBin: string;        // 库位
  batchNo: string;
  quantity: number;
  unit: string;
  lastMovement: Date;
  blockStatus: 'FREE' | 'BLOCKED' | 'QUARANTINE';
}

// 拣货作业
interface Picking {
  pickingId: string;
  salesOrder: string;
  items: PickingItem[];
  status: 'RELEASED' | 'PICKING' | 'PICKED' | 'SHIPPED';
}

// 盘点管理
interface PhysicalInventory {
  piId: string;
  storageLocation: string;
  countDate: Date;
  status: 'PLANNED' | 'COUNTING' | 'RECORDED' | 'CLEARED';
  variances: InventoryVariance[];
}
```

#### 3.3.3 实施计划

| 周次 | 任务 | 交付物 |
|------|------|--------|
| Week 1 | 收货作业（GR） | 收货确认页面 |
| Week 2 | 上架管理 | 上架任务页面 |
| Week 3 | 库存管理 | 库存查询/调整 |
| Week 4 | 拣货发货 | 拣货单/发货确认 |

---

## 四、Phase 2: 核心打通方案

### 4.1 生产执行完整化

#### 4.1.1 问题诊断

| 当前状态 | 问题 | 影响 |
|----------|------|------|
| 工单仅 4 条 mock | 无真实工单流转 | 生产计划无法执行 |
| 投料管理为空路由 | 物料无法领用 | 库存与生产脱节 |
| 完工汇报缺失 | 成品无法入库 | 库存数据不完整 |
| 无工序管理 | 5 道工序无法追踪 | 在制品不可见 |

#### 4.1.2 解决方案

```typescript
// 工序管理
interface Operation {
  operationId: string;
  operationName: string;
  workCenter: string;       // 工作中心
  standardTime: number;      // 标准工时(分钟)
  tools: string[];          // 使用工具
}

// 生产订单
interface ProductionOrder {
  poId: string;
  product: string;           // 产品
  quantity: number;           // 计划数量
  salesOrder?: string;       // 关联销售订单
  bomId: string;            // BOM
  routingId: string;        // 工艺路线
  status: 'RELEASED' | 'IN_PROGRESS' | 'COMPLETED' | 'CLOSED';
  plannedStart: Date;
  plannedEnd: Date;
  operations: POOperation[]; // 工序明细
}

interface POOperation {
  operationId: string;
  status: 'PENDING' | 'IN_PROGRESS' | 'COMPLETED';
  workCenter: string;
  plannedStart: Date;
  actualStart?: Date;
  completedQty?: number;
  scrapQty?: number;
}

// 投料记录
interface MaterialIssue {
  issueId: string;
  productionOrder: string;
  material: string;
  quantity: number;
  batchNo: string;
  issueDate: Date;
  issueType: 'NORMAL' | 'ADDITIONAL' | 'RETURN';
}

// 完工汇报
interface Completion {
  completionId: string;
  productionOrder: string;
  operationId: string;
  completedQty: number;
  scrapQty: number;
  inspectionResult: 'PASS' | 'FAIL';
  storageLocation: string;
}
```

---

### 4.2 ERP 系统集成

#### 4.2.1 问题诊断

| 缺失项 | 当前状态 | 影响 |
|--------|----------|------|
| ERP 接口 | 零实现 | 数据无法同步 |
| EDI 集成 | EDIProcessingService 为空类 | IKEA 订单无法接入 |
| 条码/RFID | 无集成 | 仓库作业效率低 |

#### 4.2.2 解决方案

```typescript
// ERP 集成服务
interface ERPIntegration {
  // 订单同步
  syncSalesOrders(): Promise<void>;
  syncPurchaseOrders(): Promise<void>;
  // 库存同步
  syncInventory(): Promise<void>;
  syncMaterialMaster(): Promise<void>;
  // 财务同步
  syncGoodsMovements(): Promise<void>;
}

// EDI 报文处理
interface EDIMessage {
  messageType: 'ORDERS' | 'DESADV' | 'INVOIC';
  sender: string;
  receiver: string;
  content: any;
  parse(): Promise<void>;
  generateResponse(): Promise<void>;
}

// 数据同步监控
interface SyncMonitor {
  syncId: string;
  entityType: 'ORDER' | 'INVENTORY' | 'MATERIAL';
  direction: 'INBOUND' | 'OUTBOUND';
  status: 'PENDING' | 'IN_PROGRESS' | 'COMPLETED' | 'FAILED';
  recordsTotal: number;
  recordsSuccess: number;
  recordsFailed: number;
  errorMessage?: string;
}
```

---

### 4.3 MRP 引擎完善

#### 4.3.1 问题诊断

| 当前状态 | 问题 | 影响 |
|----------|------|------|
| 仅 3 条 mock 数据 | 无真实 BOM 展开 | MRP 结果无效 |
| 无周期/批量约束 | 计算结果不实用 | 采购建议不可行 |

#### 4.3.2 解决方案

```typescript
// MRP 净需求计算
interface MRPResult {
  material: string;
  grossRequirement: number;   // 毛需求
  scheduledReceipts: number;  // 计划收货
  projectInventory: number;   // 预计库存
  netRequirement: number;     // 净需求
  plannedOrders: PlannedOrder[]; // 计划订单
}

interface PlannedOrder {
  orderId: string;
  orderType: 'PO' | 'MO' | 'TO';
  quantity: number;
  orderDate: Date;
  deliveryDate: Date;
  action: 'CREATE' | 'RELEASE' | 'DELAY' | 'CANCEL';
}

// BOM 展开
interface BOMExplosion {
  parentMaterial: string;
  targetDate: Date;
  requiredQuantity: number;
  components: ExplosionComponent[];
}

interface ExplosionComponent {
  component: string;
  quantity: number;          // 用量
  availableStock: number;    // 可用库存
  netRequirement: number;    // 净需求
  plannedOrders: PlannedOrder[];
}
```

---

## 五、Phase 3: 高级能力方案

### 5.1 多工厂协同

| 功能 | 描述 | 豪江场景 |
|------|------|----------|
| 三厂库存可见性 | 青岛/苏州/泰国统一视图 | 跨厂调拨 |
| 产能协同 | 泰国 43% → 70% 提升 | 产能平衡 |
| 跨厂调拨 | 调拨单/物流/入库 | 需求平衡 |

### 5.2 逆向物流

| 功能 | 描述 | SCOR 域 |
|------|------|---------|
| RMA 退货 | 客户退货处理 | Return |
| 退供处理 | 供应商退货 | Return |
| 返修管理 | 维修/翻新 | Return |
| 产品召回 | 召回管理 | Return |

### 5.3 合规管理

| 功能 | 描述 | 豪江场景 |
|------|------|----------|
| CE 认证管理 | 欧洲出口合规 | IKEA |
| 医疗器械合规 | 瑞哈质量要求 | 医疗产品 |
| 供应商审计 | 合规审计 | 供应商管理 |

---

## 六、实施路线图

### 6.1 详细时间表

```
┌─────────────────────────────────────────────────────────────────────────┐
│ Phase 1: 基础补全（1个月）                                                │
├─────────────────────────────────────────────────────────────────────────┤
│ Week 1  │ MDM数据模型设计 → 物料主数据页面开发                            │
│ Week 2  │ BOM管理 → 供应商档案管理                                       │
│ Week 3  │ IQC检验流程 → NCR处理流程                                      │
│ Week 4  │ WMS收货 → 上架管理                                            │
├─────────────────────────────────────────────────────────────────────────┤
│ Phase 2: 核心打通（2-3个月）                                              │
├─────────────────────────────────────────────────────────────────────────┤
│ Week 5-6│ 生产工单 → 投料记录 → 完工汇报                                  │
│ Week 7-8│ ERP接口开发 → EDI集成 → 数据同步                               │
│ Week 9-10│ MRP引擎完善 → BOM展开 → 计划下达                              │
├─────────────────────────────────────────────────────────────────────────┤
│ Phase 3: 高级能力（3-6个月）                                              │
├─────────────────────────────────────────────────────────────────────────┤
│ Week 11-14│ 多工厂协同 → 逆向物流 → 合规管理                              │
│ Week 15-20│ 优化调优 → 培训推广 → 正式上线                                │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 验收标准

| 阶段 | 验收项 | 标准 |
|------|--------|------|
| Phase 1 | 主数据完整度 | 100+ 物料，10+ 供应商 |
| Phase 1 | 质量管理闭环 | IQC/OQC/NCR 流程可用 |
| Phase 1 | WMS 基础作业 | 收货/上架/发货可用 |
| Phase 2 | ERP 数据同步 | 关键数据自动同步 |
| Phase 2 | MRP 运算 | 基于真实 BOM 计算 |
| Phase 3 | 多工厂协同 | 三厂库存可见 |
| Phase 3 | 逆向物流 | RMA 流程可用 |

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| ERP 接口复杂 | 高 | 高 | 分阶段实施，先核心数据 |
| 数据质量差 | 高 | 中 | 建立数据清洗流程 |
| 用户接受度 | 中 | 中 | 培训 + 渐进推广 |
| 性能问题 | 中 | 高 | 预测试 + 监控 |

---

## 八、资源需求

### 8.1 团队配置

| 角色 | 人数 | 职责 |
|------|------|------|
| 项目经理 | 1 | 整体协调 |
| 后端开发 | 3 | 业务逻辑/接口 |
| 前端开发 | 2 | UI/交互 |
| 测试工程师 | 1 | 质量保障 |
| 业务顾问 | 1 | 需求澄清 |

### 8.2 基础设施

| 项目 | 规格 | 数量 |
|------|------|------|
| 开发服务器 | 8C16G | 1 |
| 测试服务器 | 16C32G | 1 |
| 数据库 | PostgreSQL | 1 |

---

**文档版本**: v1.0  
**最后更新**: 2026-02-15  
**状态**: 待评审
